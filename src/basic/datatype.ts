let bool: boolean = true

let a: number | undefined = 23

let arr1: Array<number | string> = ['2', 1]

let add = (x: number, y: number): number => x + y

let compute: (x: number, y: number) => number

let obj: { x: number } = {x: 1}
obj.x = 1

let un: undefined = undefined
a= un

let noReturn = () => {}

let error = () => {
    throw new Error('sss')
}
